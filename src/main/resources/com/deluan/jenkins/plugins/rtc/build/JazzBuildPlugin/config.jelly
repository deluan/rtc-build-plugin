<j:jelly xmlns:j="jelly:core"
         xmlns:f="/lib/form">

    <f:section title="Rational Team Concert Build System">
        <f:entry title="Build toolkit directory">
            <f:textbox name="rtc.buildToolkit" value="${it.buildToolkit}"
                       checkUrl="'${rootURL}/plugin/rtc-build/checkBuildtoolkit?value='+escape(this.value)"/>
        </f:entry>
    </f:section>
</j:jelly>
